[package]
name = "rstat-core"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
statrs = "0.18"
thiserror = "2"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
proptest = "1"
